{
  "name": "rail-crossing-backend",
  "version": "2.0.0",
  "description": "Level crossing barrier prediction backend — LDBSVWS + Schedule + TD",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "TZ=UTC node test/time-utils.test.js && TZ=Europe/London node test/time-utils.test.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "stompit": "^1.0.0"
  }
}
